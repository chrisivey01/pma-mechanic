import React from 'react';
import { connect } from "react-redux";

const VehicleUpgrades = () => {
    return(
        <div>
            test
        </div>
    )
}

const mapStateToProps = state => {

}

export default connect(mapStateToProps)(VehicleUpgrades);